rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /players/{id} { allow read, write, delete: if true; }
    match /rallyProfiles/{id} { allow read, write, delete: if true; }
    match /activeRallies/{id} { allow read, write, delete: if true; }
    match /sentRallies/{id} { allow read, write, delete: if true; }
  }
}
